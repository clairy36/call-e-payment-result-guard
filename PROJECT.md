# 从电话结论到业务状态：付款结果校验

## 对产品与社区的理解

CALL-E 将电话执行和结构化结果获取提供给开发者，使业务流程可以通过电话完成提醒、跟进、确认和信息收集。SDK、认证、API、通话执行及 Provider 控制属于上游；社区负责围绕这些能力提供可复用的 Skills、Apps、Plugins、模板和安全模式。

研究以 2026-09-20 的仓库快照为依据。社区 README 和 Roadmap 将其定位为早期工作流社区，鼓励小型、易试用、默认可离线验证的贡献。这意味着值得补齐的不是另一套完整财务平台，而是帮助现有应用可靠消费电话结果的一小段公共能力。该判断针对研究快照，不宣称覆盖全部外部项目或此后的仓库变化。

## 问题、用户与价值

目标用户是将 CALL-E 结果接入账单提醒、付款承诺跟踪和失败扣款跟进的应用开发者，以及需要解释异常结果的运营人员。

核心问题分两层：电话结论是否有充分原话支持；即使结论有证据，它是否足以推进当前业务状态。例如“审批通过后再付”不是无条件承诺，“已经付款”也不是独立到账凭证，允许重试不等于成功追回收入。

机会并非只来自 Roadmap。对 kept 的本地合成输入探测发现：`1k` 被解析为 100 最小单位；`100 if approved` 被记录为普通承诺；金额字段与否定付款日期的原话矛盾时仍能记录。它们证明特定消费层输入存在缺口，不证明 CALL-E 在线经常产生这些输入或已经造成损失。Recover 则已有 advisory 和 recoveredCents=0 的保护，不能把这些原有边界算作新增功能。

价值依据是让指定不一致输入不再直接建承诺记录、让复核原因可解释，并在两个应用中复用同一校验契约；尚无回款率、成本或准确率改善的实证。

## 方案与贡献形式

贡献由一个有限范围的 Python 核心、两个薄适配器和离线 harness 组成。核心接收业务对象、结构化结论及逐轮转写，分别返回证据状态和业务可推进状态。

- kept：在既有绑定和策略检查之后、构建 Promise 之前接入。
- Recover：在鉴权和权威回查之后、保存建议之前调用相同核心；不执行扣款。
- 验证工具：复用已有 SDK 模拟、临时账本、实际路由和子进程调用路径；将模拟、适配回放和真实外部集成分开记录。

选择验证工具加 Adapter，而不是另建平台或只写提示词，是因为问题出现在结果进入业务状态的交界处，需要可执行、可追溯的规则。代码 Patch 便于查看有限改动，无需部署即可复现核心行为。

## 关键取舍与未做内容

只支持有限 en-US、USD 数字金额和明确日期表达，未知或含糊表达转复核；不承诺通用语言理解。Recover 的 Python 子进程带来本地解释器依赖，不宣称 Edge/serverless 可用。身份、号码授权和业务对象绑定由宿主负责；调度由宿主 Scheduler 负责；电话执行仍由 CALL-E 负责；到账和实际支付授权依赖独立业务系统及人员。

没有重建账单、支付、对账或 CRM 平台，没有外部部署，没有补全原宿主所有恢复与并发机制。P03 的拒绝语义虽有原话支持，当前核心仍返回 unsupported，这个限制明确保留。

## 验证过程与当前结果

当前本地 harness：核心 63 项、kept 195 项、Recover 49 项，共 307 项通过。42 个基础合成案例验证规则边界；三组基线对照复现已确认的消费层问题。两个宿主路径使用本地模拟供应商或数据库边界，不等于真实生产集成。

另进行了默认 AI 接待线路上的真实脚本电话。最终系统话术轮中，P01 未返回目标承诺句；P02 保留条件但金额由预设 100.00 变为转写/提取的 100100；P03 返回完整否认句。它们不是实际客户或真实债务。金额异常尚未结合录音定位原因。

v0.2 将三个样本脱敏后加入回放，分别报告供应商完成、目标句、金额一致性和核心结论。普通回放退出 0 表示观察结果可复现；样本覆盖检查退出 1，提示尚有目标句待补充。核心对 unclear/refused 会提前退出，因此不把这轮回放宣传成核心独立识别条件或金额的成功，也不把已有 kept 保护重复计算。

## 安全与真实世界副作用

默认演示和回归不拨电话、不执行支付、不要求凭据。既往真实通话由用户明确授权，限定测试号码、逐项拨打、不自动重拨；查询中断后只恢复同一 call_id 的查询。关闭页面或停止查询不代表已取消已提交电话。公开材料不含 API key、真实号码、原始录音或可定位账号的通话记录。

业务写入仍受宿主已有控制约束。缺证据、模糊绑定、异常校验输出不能默认为通过。任何核心结果都不是支付执行授权；声称已付必须等待独立到账证明。

## AI 使用与人工判断

AI 辅助读取仓库、对比已有工具、生成方案与代码、运行测试、分析实际转写并整理文档。用户保留问题选择、付款领域范围、跨应用复用方向、真实通话授权、接听线路和话术迭代等关键判断。生成结果通过具体输入、源码、日志和补丁核对，不把 AI 的摘要或任务完成字段当作独立证据。实际工作跨多轮研究与验证，没有将整个成果描述成在三小时内完成。

## 下一步

先补齐 P01 完整样本并核查 P02 金额音频，再评估是否扩展拒绝分类与口语金额支持；保持原始证据和修正记录。随后分别验证 Recover 的真实重试意向场景与宿主受控接入。新增规则须先有回归案例，不为了让固定样本通过而改写转写或放宽业务边界。

研究依据：仓库快照中的 README、docs/roadmap.md、AGENTS.md、docs/community-review-policy.md，以及 kept / Recover 的源码和本地验证。上游参考仓库为 CALLE-AI/call-e-integrations 与 CALLE-AI/awesome-phone-call-agents；版本与补丁基线见运行说明。
